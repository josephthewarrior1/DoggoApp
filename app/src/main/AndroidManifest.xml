<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.doggo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Doggo"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.Light.NoActionBar">

        <!-- Intro Activity (Launcher) -->
        <activity
            android:name="com.example.doggo.IntroActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth Activities -->
        <activity
            android:name="com.example.doggo.SignInActivity"
            android:exported="false" />

        <activity
            android:name="com.example.doggo.SignUpActivity"
            android:exported="false" />

        <!-- Main Activity -->
        <activity
            android:name="com.example.doggo.MainActivity"
            android:exported="false" />

        <!-- Home Activities -->
        <activity
            android:name="com.example.doggo.Home.HomeActivity"
            android:exported="false" />

        <activity
            android:name="com.example.doggo.Home.AddDogProfileActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name="com.example.doggo.Home.DogProfileDetailActivity"
            android:exported="false" />

    </application>

</manifest>